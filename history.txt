# AI-Coding-Share 会话历史

本文件记录用户与 AI 助手围绕本项目的每一轮交互。新增内容追加到文件末尾。

格式：
  ## [round N] YYYY-MM-DD HH:MM (UTC+8) - 主题
  - user: 用户诉求摘要
  - actions: AI 实际执行的关键动作 / 改动的核心文件
  - artifacts: 产物（commit 短哈希、新生成的文件、外部链接等）

================================================================================

## [round 1] 2026-05-12 22:39 (UTC+8) - 初始需求与澄清
- user: 提出"反传统 PPT 粒子演讲"完整需求（见 prompt.txt 与 speech.txt）：
  React + WebSocket，按中文句号切分每句生成 SVG 图示并由粒子拟合，后台
  控制端 + 多观众端同步直播，观众端字幕、后台预览/进度/时间估算等。
- actions: 在 Plan 模式下读取 speech.txt、确认仓库基本为空；用 AskQuestion
  确认两点：标题作为单独封面页（是）、采用 Vite+React+TS+Node WS 默认栈
  （是）；调用 CreatePlan 提交实施计划，用户接受。
- artifacts: .cursor/plans/particle_presentation_1f1f22f1.plan.md

## [round 2] 2026-05-12 22:44 (UTC+8) - 实施粒子演讲系统
- user: 让助手按计划实施。
- actions: 从零搭建 presentation/ 子工程：
    * package.json / tsconfig*.json / vite.config.ts / index.html / .gitignore
    * server/index.ts — Node http + WebSocket，维护权威状态、广播
      { currentIndex, seed, transitionStartedAt, slideDurations, ... }
    * src/slides.ts — 通过 ?raw 导入 ../../speech.txt，按段落+中文句号切分，
      生成 79 张幻灯片，按 SCENE_PLAN 映射到 sceneKind / sceneParams
    * src/particle/sceneShapes.ts — 72 套 SVG 图示模板（line/rect/circle/
      arrow/star/wave/spiral/brain/gear/bigDigit 等基础原语 + 每个 sceneKind
      对应的 sceneFn），用 DOM SVG getPointAtLength 把所有路径采样为
      ~2200 个目标点，按长度比例分配
    * src/particle/ParticleScene.tsx — Canvas 粒子渲染，每帧用
      easeInOutCubic + transitionStartedAt + timeOffsetMs 计算 t，叠加
      正弦扰动；场景切换时基于 seed 做 mulberry32 洗牌重新分配目标
    * src/sync/wsClient.ts — useSync hook，支持指数退避重连、serverTime
      校准时钟差
    * src/pages/AudiencePage.tsx / AdminPage.tsx — 观众/后台 UI，含双预览
      预览、进度条、已用时间、估算剩余时间、演讲稿高亮、键盘快捷键
    * src/main.tsx / App.tsx / styles.css
  完成后跑 npm run typecheck（通过）、npm run build（212 KB），启动生产
  服务并用 scripts/ws-smoke.mjs 验证 admin+2 observer 同步成功
  （SYNC_OK true）。
- artifacts: presentation/ 工程整体；smoke 输出确认 3 端 currentIndex /
  seed 一致。

## [round 3] 2026-05-12 23:10 (UTC+8) - 后台进程清理通知
- user: 系统通知"启动生产服务器"/"启动 dev 模式"两个 background 任务终止。
- actions: 告知用户这两个进程是验证完成后助手主动 kill 的清理操作，端口
  5173/5174 已释放，无需后续动作。
- artifacts: 无文件改动。

## [round 4] 2026-05-12 23:23 (UTC+8) - 根目录 README
- user: 请为项目生成一个 readme.md 文件。
- actions: 在仓库根创建 README.md，作为顶层入口：仓库导览、快速启动、
  mermaid 同步原理图（speech.txt → slides.ts → ParticleScene；admin → WS
  Server → 观众 A/B）、关键特性、目录结构，并链接到子项目
  presentation/README.md。
- artifacts: README.md (82 行)。

## [round 5] 2026-05-12 23:28 (UTC+8) - 初始化 Git 并推送 GitHub
- user: 帮我创建 git 仓库并推送到 git@github.com:YicongCao/AI-Coding-Share.git
- actions: 创建根目录 .gitignore（node_modules / dist / *.tsbuildinfo / .DS_Store
  等）；git init 后用 git branch -m main 切到 main；用 git rm --cached 把
  tsbuildinfo 移出索引，并在 presentation/.gitignore 增补 *.tsbuildinfo；
  提交 24 个文件；ssh -T git@github.com 验证身份；添加 origin 并
  git push -u origin main。
- artifacts: 首次提交 1c3e049 "feat: 粒子演讲系统初版"；远端
  https://github.com/YicongCao/AI-Coding-Share 上线。

## [round 6] 2026-05-12 23:31 (UTC+8) - 解释演讲稿为何未硬编码
- user: 演讲稿文本我在源码里搜不到，是没找对地方吗？
- actions: 解释 src/slides.ts 通过 Vite 的 `?raw` 在构建期把 ../../speech.txt
  拼成字符串，运行时 parseParagraphs + splitByPeriod 切句，因此源码里只
  能搜到 ?raw import；构建产物 dist/assets/*.js 里能搜到原文（已演示
  grep 命中）；同时说明 SCENE_PLAN 是显式的"段-句 → sceneKind"映射，需
  要在该数组同步维护。
- artifacts: 无文件改动；解释性回复。

## [round 7] 2026-05-12 23:33 (UTC+8) - 后台展示场景代号
- user: 在 admin 页面显示每页的"代号"，方便排练时与 AI 协同改图示/台词。
- actions: AdminPage.tsx 增加 formatSceneCode(kind, params) 工具；CURRENT
  SENTENCE 上方新增 scene-badge（#index/total + sceneKind(params)，点击复
  制）；演讲稿右栏每个句子末尾追加 .sentence-code 内联 chip（#N·sceneKind，
  悬停 tooltip 显示完整参数，单独点击复制不触发跳页）；预览卡片右下角标
  也改为相同样式并展示带参数代号。styles.css 新增 .scene-badge /
  .sentence-code / 优化 .preview-index 与 .current-info 等样式。
  npm run typecheck + npm run build 全部通过；ReadLints 无错。
- artifacts: 提交 a4bf343 "feat(admin): 在后台显示每页的场景代号..." 已
  push 到 origin/main。

## [round 8] 2026-05-12 23:35 (UTC+8) - 建立长期会话记忆与自动推送
- user: 把现有对话历史写入 history.txt，今后每轮都追加；以及每次改完都默
  认 commit+push。
- actions: 撰写本 history.txt（含 round 1-8 摘要）；新建
  .cursor/rules/session-log-and-push.mdc（alwaysApply: true），把"每轮
  必须追加 history.txt 并 commit+push 到 origin/main"固化为项目规则；
  本轮也按该规则执行。
- artifacts: 提交 808d325 "chore(history): round 8 建立会话日志与自动推送规则"。

## [round 9] 2026-05-12 23:43 (UTC+8) - 在线/活跃统计 + admin 登录密码
- user: admin 显示当前 WS 在线数；观众端前台时每 5s ping，admin 实时看到
  每页活跃人数并 localStorage 持久化（避免误关丢数据）；为 admin 加默认
  密码 nex2026 防止听众闯入。
- actions:
  * server/index.ts 重写：每连接生成 clientId；新增 ClientMessage 类型
    含 identify/ping；ClientRecord 跟踪 role 与 lastPingAt（8.5s 活跃窗口）；
    computeStats 输出 { totalClients, adminClients, audienceClients,
    activeAudience }；hello 携带 clientId；新增 stats 帧，仅广播给
    role === "admin" 的客户端；每 2.5s 定时 broadcastStats，命令处理后
    与连接变化时也立即广播。
  * src/sync/wsClient.ts：useSync 接受 { role, pingIntervalMs? }；open 后
    发 identify；audience 角色监听 visibilitychange，仅在 visible 时每
    5s 发 ping；新暴露 stats 与 clientId。
  * src/pages/AudiencePage.tsx 改为 useSync({ role: 'audience' })。
  * src/pages/AdminPage.tsx 大改：拆出 LoginGate（密码 nex2026，错误时
    shake 动画 + 红色提示，认证存 localStorage:nex-admin-auth-v1）与
    AdminConsole；useSync({ role: 'admin' })；顶部新增 stats-row 三块
    chip（管理端/观众端/活跃 with 脉冲点）；progress 下方新增
    engagement-block，79 根 bar 按 engagement[i]/max 显示高度，当前页
    橙金色高亮，点击跳页，悬停 tooltip 显示活跃度与文本；activeAudience
    每秒按 dt 累加到 engagement[currentIndex]，每 1.5s 落 localStorage:
    nex-admin-engagement-v2；新增"清空统计"与"退出"按钮（btn-subtle）。
  * src/styles.css：新增 stats-row/stats-chip/live-dot 脉冲、
    engagement-block/engagement-bars/engagement-bar、login-root/-card/
    shake 动画、btn-subtle、preview-placeholder 等样式。
  * scripts/ws-stats-smoke.mjs：1 admin + 2 audience 端到端测：identify
    后 stats 正确（activeAudience=2）、admin/audience 都收到 state、
    audience 不收 stats、断连/重 ping 后计数刷新。
  * 验证：npm run typecheck 通过；npm run build 218.35 KB JS / 10.15 KB
    CSS；ws-stats-smoke 输出 STATS_SMOKE_OK；老 ws-smoke.mjs 仍
    SYNC_OK true。
- artifacts: 提交 54b79d0 "feat(admin): 在线/活跃统计、每页活跃度直方图
  与密码登录"，推送到 origin/main。

## [round 10] 2026-05-12 23:45 (UTC+8) - 后台进程清理通知
- user: 系统通知 round 9 验证用的生产服务（task 541920）被 aborted。
- actions: 告知用户该 abort 是我在 ws-stats-smoke 完成后主动 kill 40409
  执行的清理，端口 5174 已释放，无需后续处置。
- artifacts: 无业务改动；本轮仅追加 history。

## [round 11] 2026-05-12 23:46 (UTC+8) - 建立 changelog 与梳理每轮例行
- user: 生成 changelog.txt，回填到目前每个版本的标题与详情概要；今后每次
  改代码都要追加；并询问"每轮对话完默认要做哪些事情"。
- actions:
  * 新建 changelog.txt（按版本倒序）：v0.1.0 初版粒子演讲系统、v0.2.0
    admin 场景代号、v0.2.1 history+rule 内务、v0.3.0 在线统计+登录、
    v0.3.1 round 10 history 修订。每个版本含 commits 短哈希。
  * 更新 .cursor/rules/session-log-and-push.mdc：把"每轮触碰业务代码时
    在 changelog.txt 顶部追加新版本"纳入规则；保留 history.txt 每轮无
    条件追加 + commit+push 不变；明确版本号 SemVer 粗粒度分级、
    conventional commit prefix 约定、push 冲突处置、不修改 git config /
    不 force push / 不跳过 hook 等边界。
  * 在响应正文中按用户提问列出每轮必做的 6 件事（history 必做、
    changelog 仅代码变更时做、commit、push、静默执行、git 安全护栏）。
  * 因为 history 写在 commit 之前哈希未知，本轮意外触发"先有鸡还是
    先有蛋"问题，先 push 了主提交，再用 chore 补丁回填真实哈希
    （详见 round 12 把这一教训沉淀到 rule）。
- artifacts: 提交 51915d2 "docs(changelog): 建立 changelog.txt 并把追加约束
  写进项目规则" + 补丁 5629bd8 "chore(history): 回填 round 11 真实 commit
  哈希" 推送到 origin/main。

## [round 12] 2026-05-12 23:50 (UTC+8) - 把 commit hash 鸡生蛋的解法写进规则
- user: 把刚才"先 commit 再回填哈希"的结论也写进 rules，避免下次上下文压
  缩后再领悟一遍。
- actions:
  * 第一版：在 .cursor/rules/session-log-and-push.mdc 拆出"## commit hash
    鸡生蛋问题与推荐工作流"章节，按"单次 commit + amend"思路写。dogfood
    该流程：history 用 `<pending>` 占位 → commit C1=f270584 → 回填 →
    `commit --amend` → push。
  * dogfood 当场暴露 BUG：`commit --amend` 重新生成新哈希
    （f270584 → 61226d0），history 里写的 f270584 立刻变成远端不存在的
    孤儿引用，自指悖论无法绕过。
  * 第二版（本轮收尾）改方案为 **Pattern A1（哈希延后一轮回填）**：
    - 本轮 artifacts 用 `<pending>` 占位 → commit → push → **不 amend**；
    - **下一轮**编辑 history 时第一件事是 `git log --oneline -10` 把上轮
      所有 `<pending>` 回填为真实短哈希，再追加新内容；
    - 保留 Pattern A3（两次提交同轮闭合）作备选；
    - 新增反模式条目：用 amend 合并自指 history、`push --force`、编假哈希、
      回填拖到下下轮。
  * 同步检查发现 round 8/9（808d325/54b79d0）当时的 history 确实写的是
    "哈希见 git log / 下次更新"，正是 Pattern A1 的事实做法，写进规则
    末尾"历史经验"小节作为论据。
  * 同步修复 round 12 自身的孤儿哈希：把上文 f270584 改为 61226d0
    （Pattern A3 的事后补丁应用）。
- artifacts: 提交 61226d0 "docs(rules): 把 commit hash 鸡生蛋解法写进 session
  log 规则"（初版，amend 失败留下的实际 push 版本）+ 补丁 <pending>
  "docs(rules): 修正鸡生蛋方案为 Pattern A1 并回填 round 12 孤儿哈希"
  推送到 origin/main；后一条哈希按 A1 在下轮回填。
