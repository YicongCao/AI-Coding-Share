---
description: 每轮对话结束前必须追加 history.txt、必要时追加 changelog.txt，并 commit+push 到 origin/main
alwaysApply: true
---

# 会话日志、版本日志与自动推送

本项目（AI-Coding-Share）需要完整可追溯的迭代记录。每一轮（即每次回复用户前）结束前按下面顺序执行；正常情况下静默进行，不在响应正文里报告这套流水线（除非用户问起，或某一步失败需要他介入）。

## 每轮必做

### 1. 追加 `history.txt`（无条件，每轮都做）

把这一轮交互追加到仓库根的 `history.txt`，沿用文件内既有格式（见该文件顶部说明）。`round N` 取上一条的下一个整数；时间用当前可用的 timestamp，时区写 `(UTC+8)`：

```
## [round N] YYYY-MM-DD HH:MM (UTC+8) - 主题
- user: 用户诉求摘要（1-3 句）
- actions: 实际做了什么、改动的关键文件、跑过的校验
- artifacts: commit 短哈希、新生成的文件、外部链接等
```

### 2. 追加 `changelog.txt`（仅当本轮改了业务代码）

如果本轮**触碰了除 `history.txt` / `changelog.txt` / 规则文件以外的任何代码或资源**，必须在仓库根 `changelog.txt` 顶部插入一条新版本：

- 版本号遵循 SemVer 粗粒度：
  - MAJOR 不兼容架构变更（罕见，须用户确认再升）
  - MINOR 新增用户可见功能
  - PATCH 内务、文档、构建脚本、修复
- 标题简短点明本次主题；正文按 `### 新增 / 修复 / 内务 / 重构` 等分类列出条目，末尾标注当次 `commits:`。
- 顺序：最新版本永远在文件顶部、说明性前言之下。

纯问答 / 解释 / 仅更新 history 或规则文件的轮次**不要**改 changelog。

### 3. Commit + 回填哈希 + Push

详见下文「commit hash 鸡生蛋问题与推荐工作流」。一句话：**先 commit，再用 `git log -1 --format=%h` 拿哈希回填 history.txt，再 `git commit --amend --no-edit`，最后 `git push`，每轮只留一条干净提交**。

- `<type>` 使用 conventional 风格：`feat` / `fix` / `chore` / `docs` / `refactor` / `perf` 等
- `git push` 是 `git push origin main`

## commit hash 鸡生蛋问题与推荐工作流

**问题**：`history.txt` 的 `artifacts:` 行需要写本轮 commit 的短哈希，但哈希只在 `git commit` 之后才存在。直接写"先 commit 再补 history"会导致每轮被迫拆成 2 条提交，污染历史；而"先 push 再发现需要回填"又会诱导你想用 `--force` 修正——后者绝对禁止。

**推荐做法：单次提交 + amend（push 之前 amend 是安全的）**

1. 完成全部代码 / 资源改动。
2. 写 `changelog.txt`（若适用）；写 `history.txt` 时 `artifacts:` 行先用占位符，例如：
   ```
   - artifacts: 提交 <pending> "<commit title>" 推送到 origin/main。
   ```
3. `git add -A && git commit -m "<type>(<scope>): <subject>"`
4. `git log -1 --format=%h` 拿到 7 位短哈希。
5. 用 `StrReplace` 把 `history.txt` 里的 `<pending>` 替换为真实哈希。
6. `git add history.txt && git commit --amend --no-edit`（**只在还没 push 时**才允许 amend；这一步合并到上一条提交，不留补丁记录）。
7. `git push origin main`。

> 关键点：amend 永远发生在 push **之前**，所以不违反"不要 amend 已 push 的提交"。

### Fallback：业务提交已经 push 出去了

如果你在 amend 之前不慎 push 了，**禁止** `git push --force`。改用补丁提交：

1. 在 `history.txt` 里把占位符换成真实哈希。
2. `git add history.txt && git commit -m "chore(history): 回填 round N 真实 commit 哈希"`
3. `git push origin main`

本轮会留下两条提交（业务 + 补丁），可接受但不优雅；下次回到推荐做法。

### 反模式（不要做）

- ❌ 写 history 时直接编一个假哈希凑数
- ❌ 跳过 history 的 artifacts 行
- ❌ 已 push 后用 `git commit --amend && git push --force` 改主线
- ❌ 把哈希回填拖到下一轮再做（会导致下一轮 history 提到上一轮的事，引用乱套）

## 边界情况

- 仅问答 / 解释 / 系统通知确认时，也要更新 `history.txt` 并 commit+push（保留完整记录）；不动 changelog
- 若用户在本轮明确说"不要 commit"或"不要 push"，遵循当轮指令，但仍要把"用户拒绝提交"这一事实写进 history.txt
- 若 `git push` 因冲突 / 鉴权失败，先 `git pull --rebase` 再推；仍失败则向用户如实说明并请求处置
- 不修改 git config，不 force push 到 main，不跳过 hook

## 文件位置

- `history.txt` 在仓库根，逐轮追加
- `changelog.txt` 在仓库根，按版本倒序排列，最新版本在顶部
- 本规则在 `.cursor/rules/session-log-and-push.mdc`
